import React from "react";

function SideBar() {
  return (
    <div
      style={{
        float: "right",
        background: "red",
        width: "100px",
        height: "100vh",
      }}
    >
      SideBar
    </div>
  );
}

export default SideBar;
